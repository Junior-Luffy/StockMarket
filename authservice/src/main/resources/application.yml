server:
  port: 8082

spring:
  application:
    name: auth-service

eureka:
  client:
    service-url:
           defaultZone: http://127.0.0.1:8100/eureka
    register-with-eureka: true
    fetch-registry: true

feign:
  hystrix:
    enabled: false
